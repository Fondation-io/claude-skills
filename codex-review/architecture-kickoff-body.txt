A hand-drawn French diagram on a single page, fully hand-lettered.

SCENE PLAN: A landscape diagram in two halves, title hand-lettered across the top: "KICKOFF — QUEL DOC, QUELLE SESSION". The LEFT half is a vertical funnel of four stacked trays read top to bottom, each tray tried before falling through to the next, joined by short downward arrows: first tray "① historique de la session" drawn as a speech bubble trail, second tray "② argument en cours" drawn as an index card, third tray "③ disque : git status" drawn as a folder, fourth tray "④ une seule question" drawn as a question mark in a bubble. The funnel empties into one sheet at the bottom labelled "doc + type + feature", with the annotation "annoncé, jamais silencieux" beside it. The RIGHT half shows three railway-like tracks converging on a single large spool of thread labelled "thread Codex": the first track starts from one document icon and is labelled "même doc → resume", the second track starts from three small chained documents and is labelled "spec → plan → impl : même fil", the third track starts from a document marked with a star and is labelled "autre feature → neuf". A small broken thread beside the spool carries the annotation "fil mort → repartir + log". At the BOTTOM CENTER, a triangle of three corners joined by double-headed arrows shows the coupling: corner one is the document sheet labelled "doc", corner two is the index card labelled "sessions.json", corner three is the spool labelled "thread_id"; inside the triangle the annotation "une entrée par feature".

ALL TEXT IS HAND-LETTERED BY YOU, in French, copied EXACTLY, character for character, accent for accent. Do not translate, shorten or invent a single word. The exact texts are: "KICKOFF — QUEL DOC, QUELLE SESSION", "① historique de la session", "② argument en cours", "③ disque : git status", "④ une seule question", "doc + type + feature", "annoncé, jamais silencieux", "thread Codex", "même doc → resume", "spec → plan → impl : même fil", "autre feature → neuf", "fil mort → repartir + log", "doc", "sessions.json", "thread_id", "une entrée par feature".

COLOUR WORDS: hand-letter "même fil" in leaf green, "neuf" in warm coral, "thread_id" in electric blue, "annoncé, jamais silencieux" in violet, "feature" (in "doc + type + feature") in sunny yellow. Every other word is lettered in black ink.

THE DRAWING IS YOURS: the idea is that the skill first guesses which document you mean by trying four sources in strict order, then decides whether the ongoing conversation with the critic continues on the same thread or starts a new one — and one small card file couples the three things together. Keep recognisable: the four-tray funnel, the spool of thread, the three converging tracks, the broken thread, the coupling triangle. Framing, scale and the exact gesture of each stroke are yours.

