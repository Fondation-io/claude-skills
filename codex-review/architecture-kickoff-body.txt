A hand-drawn English diagram on a single page, fully hand-lettered.

SCENE PLAN: A landscape diagram in two halves, title hand-lettered across the top: "KICKOFF: WHICH DOC, WHICH SESSION". The LEFT half is a vertical funnel of four stacked trays read top to bottom, each tray tried before falling through to the next, joined by short downward arrows: first tray "① just written in this conversation" drawn as a speech bubble trail, second tray "② argument in progress" drawn as an index card, third tray "③ on disk: git status" drawn as a folder, fourth tray "④ one question" drawn as a question mark in a bubble. The funnel empties into one sheet at the bottom labelled "doc + type + feature", with the annotation "announced, never silent" beside it. The RIGHT half shows three railway-like tracks converging on a single large spool of thread labelled "Codex thread": the first track starts from one document icon and is labelled "same doc → resume", the second track starts from three small chained documents and is labelled "spec → plan → impl: same thread", the third track starts from a document marked with a star and is labelled "other feature → new thread". Beside the spool, a small broken thread carries the annotation "thread truly dead → start over + log", and next to it a small stop sign carries the annotation "passing error → stop, keep the state". At the BOTTOM CENTER, a triangle of three corners joined by double-headed arrows shows the coupling: corner one is the document sheet labelled "doc", corner two is the index card labelled "sessions.json", corner three is the spool labelled "thread_id"; inside the triangle the annotation "one entry per feature".

ALL TEXT IS HAND-LETTERED BY YOU, in English, copied EXACTLY, character for character. Do not translate, shorten or invent a single word. The exact texts are: "KICKOFF: WHICH DOC, WHICH SESSION", "① just written in this conversation", "② argument in progress", "③ on disk: git status", "④ one question", "doc + type + feature", "announced, never silent", "Codex thread", "same doc → resume", "spec → plan → impl: same thread", "other feature → new thread", "thread truly dead → start over + log", "passing error → stop, keep the state", "doc", "sessions.json", "thread_id", "one entry per feature".

COLOUR WORDS: hand-letter "same thread" in leaf green, "new thread" in warm coral, "thread_id" in electric blue, "announced, never silent" in violet, "feature" (in "doc + type + feature") in sunny yellow. Every other word is lettered in black ink.

THE DRAWING IS YOURS: the idea is that the skill first guesses which document you mean by trying four sources in strict order, then decides whether the ongoing conversation with the critic continues on the same thread or starts a new one — a broken thread only restarts when it is truly dead, and one small card file couples the three things together. Keep recognisable: the four-tray funnel, the spool of thread, the three converging tracks, the broken thread, the stop sign, the coupling triangle. Framing, scale and the exact gesture of each stroke are yours.

