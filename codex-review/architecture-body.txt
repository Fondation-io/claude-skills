A hand-drawn English diagram on a single page, fully hand-lettered.

SCENE PLAN: A landscape architecture diagram read left to right. Title hand-lettered across the top: "THE CODEX-REVIEW SKILL". The LEFT third is a vertical column of three small paper documents stacked top to bottom, joined by two short downward arrows: the sheet "SPEC", then the sheet "PLAN", then the sheet "IMPL (diff)" drawn as a page of code lines with plus and minus marks in the margin. Beside this column, vertically, the annotation "one feature, one session". The CENTER third is the heart and dominates the page: a large circular loop of two thick curved arrows between two actors — on the left of the loop a blank oval-headed silhouette at a keyboard labelled "Claude" with "author and referee" beneath, on the right of the loop a plain terminal window (a rectangle with a command prompt chevron inside, no logo) labelled "Codex" with "critic, read-only" beneath. The top curved arrow goes from Claude to Codex, labelled "doc + prompt". The bottom curved arrow returns from Codex to Claude, labelled "VERDICT". Just under the verdict arrow, two hand-lettered outcomes side by side: "APPROVED" and "REVISE". Under the Claude silhouette, a small mechanical ratchet wheel with its pawl, labelled "simplicity ratchet". At the top of the loop a small counter tally labelled "MAX ROUNDS". Each document of the left column sends one thin arrow into the loop. The RIGHT third is the persistent state: three small file cards drawn as index cards, labelled "sessions.json", "review-log.md" and "stream.jsonl". A double-headed arrow links the loop to the "sessions.json" card, labelled "thread_id", and under that card the small annotation "written only after a successful round"; beneath the "stream.jsonl" card the annotation "live progress". At the BOTTOM RIGHT corner, a blank oval-headed silhouette signing a sheet with a pen, labelled "human sign-off", reached by one arrow leaving the loop from the word "APPROVED".

ALL TEXT IS HAND-LETTERED BY YOU, in English, copied EXACTLY, character for character. Do not translate, shorten or invent a single word. The exact texts are: "THE CODEX-REVIEW SKILL", "SPEC", "PLAN", "IMPL (diff)", "one feature, one session", "Claude", "author and referee", "Codex", "critic, read-only", "doc + prompt", "VERDICT", "APPROVED", "REVISE", "simplicity ratchet", "MAX ROUNDS", "sessions.json", "review-log.md", "stream.jsonl", "thread_id", "written only after a successful round", "live progress", "human sign-off".

COLOUR WORDS: hand-letter "APPROVED" in leaf green, "REVISE" in warm coral, "thread_id" in electric blue, "simplicity ratchet" in violet, "one feature, one session" in sunny yellow. Every other word is lettered in black ink.

THE DRAWING IS YOURS: the idea is a bounded argument — one human-side author and one read-only critic passing the same document around a loop until it survives, while a small card file remembers the conversation across days. Keep recognisable: the three stacked documents, the circular two-arrow loop, the terminal window with its chevron, the ratchet wheel with pawl, the index cards, the signing silhouette. Framing, scale and the exact gesture of each stroke are yours.

